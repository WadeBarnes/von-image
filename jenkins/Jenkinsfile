#!groovy

node {    
    stage ("Get Latest Code") {
        checkout scm
    }

    stage ("Build The Images") {
      withPythonEnv('Python_3.6'){
        pysh 'make_image.py dev-441 --release --py36 --s2i --test --name bcgovimages/von-image'
      }
    }
}